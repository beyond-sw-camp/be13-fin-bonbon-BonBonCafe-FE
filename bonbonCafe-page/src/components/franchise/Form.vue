<template>
  <v-container>
    <v-text-field>
      
    </v-text-field>
  </v-container>
    
</template>
<script setup>

  import { ref } from 'vue'

  const first = ref(null)
  const last = ref(null)
  const email = ref(null)
  const password = ref(null)
  const terms = ref(false)
</script>